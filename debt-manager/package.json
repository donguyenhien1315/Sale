{
  "name": "debt-manager",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute debt-manager-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
